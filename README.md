# Distributed-inventory-system
Distributed inventory and message-passing system in C, built with sockets, custom protocols, concurrent handlers, and file-based realm data. Supports inter-realm communication, stock queries, pledge/order handling, and transfer coordination through a modular Unix-style architecture.
